@model FrontEnd.Models.ControlAforo

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ControlAforo</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CodControl" />
            <input type="hidden" asp-for="CodEmpresa" />
            <input type="hidden" asp-for="NumeroDia" />
            <div class="form-group">
                <label asp-for="NumeroAforo" class="control-label"></label>
                <input asp-for="NumeroAforo" class="form-control" />
                <span asp-validation-for="NumeroAforo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
